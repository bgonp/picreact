export const ROUTE_ROOT = `${window.location.protocol}//${window.location.host}`

export const ROUTE_CREATE = '/create'
export const ROUTE_HOME = '/'
export const ROUTE_LOAD = '/load/:code'
export const ROUTE_PLAY = '/play'

const ROUTES = {
  ROOT: ROUTE_ROOT,
  CREATE: ROUTE_CREATE,
  HOME: ROUTE_HOME,
  LOAD: ROUTE_LOAD,
  PLAY: ROUTE_PLAY,
}

export default ROUTES
